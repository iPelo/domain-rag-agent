{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI: backend",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--reload", "--app-dir", "backend"],
      "envFile": "${workspaceFolder}/.env",
      "justMyCode": true
    }
  ]
}
